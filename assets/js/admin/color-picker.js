/**
 * Color picker initialization for Holler Team Settings
 *
 * @package Holler_Elementor
 * @since 1.0.0
 */

(function($) {
    'use strict';
    
    // Initialize color pickers when document is ready
    $(document).ready(function() {
        $('.color-picker').wpColorPicker();
    });
    
})(jQuery);
